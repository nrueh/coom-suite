product {
    3..3 Module module
}

enumeration Module {
    attribute num ects
    attribute str group

    A = ( 6, a )
    B = ( 9, b )
}

behavior {
    require 12 = sum(m.ects for m in module if m.group = a)
}
