// "Shadow Master" Blinds example
// Provided by denkbares GmbH

product {
			num/mm 		assemblyHeight
			num/mm		passageHeight
			num.#/mm	inputWidth
			num/mm		length
			num.##/rad	inclination
}

behavior {
	// imply inclination = (125 - inputWidth) // working, because inputWidth is float?
	// imply inclination = (assemblyHeight - 8.82) // working, because 8.82 is float
	// imply inclination = (90 - passageHeight) // not working


}
